# YOLOv5 🚀 by Ultralytics, GPL-3.0 license
# ADD EO/IR dataset

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
dataset_name: ADD-EOIR-IR
path: /131_data/datasets/add_eoir/v2.1_230220  # dataset root dir
train: train_IR.txt # train images (relative to 'path')
val: test_IR.txt  # val images (relative to 'path')
test: test_IR.txt

val_anno_json: annotations/annotations_test_IR.json
test_anno_json: annotations/annotations_test_IR.json

# Classes
names:
  0: person
  1: animal
